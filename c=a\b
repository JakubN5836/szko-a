#include <iostream>
using namespace std;
int main() {
   float a, b, c;
   cout << "Podaj a: ";
   cin >> a;
   cout << "Podaj b: ";
   cin >> b;
   c = a/b;
    {
        if (b==0)
            cout << "Dzielnik jest rowny 0";
        else
            cout << "Wynik: " << c;
    }
    return 0;
}
